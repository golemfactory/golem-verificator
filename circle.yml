machine:
  python:
    version: 3.6.3
  services:
    - docker
  post:
    - pyenv global 3.6.3

dependencies:
  override:
    - pip3 install -r requirements.txt
    - python3 -m venv venv
    - python3 setup.py develop

test:
  override:
    - source venv/bin/activate
    - pytest
